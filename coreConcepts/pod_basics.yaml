apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels: 
    app: web-server
    tier: frontend
spec:
  containers:
    - name: nginx-container
      image:  nginx
  restartPolicy: Never
  resources:
  